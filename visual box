local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local Camera = workspace.CurrentCamera

local LocalPlayer = Players.LocalPlayer

local StickyHitboxes = {}

local ENABLED = true
local RUNNING = true
local MAX_DISTANCE = 20
local MAX_MATCH_DISTANCE = 10

local latched = {}
local seen = {}
local lastFolderState = nil

local function getCharacterData(player)
    if player == nil then
        return nil, nil, nil
    end

    local character = player.Character
    if character == nil then
        return nil, nil, nil
    end

    local humanoid = character:FindFirstChildOfClass("Humanoid")
    local root = character:FindFirstChild("HumanoidRootPart")

    if humanoid == nil or root == nil then
        return nil, nil, nil
    end

    if humanoid.Health <= 0 then
        return nil, nil, nil
    end

    return character, humanoid, root
end

local function getBodyAnchor(character, root)
    local part = character:FindFirstChild("UpperTorso")

    if part == nil then
        part = character:FindFirstChild("Torso")
    end

    if part == nil then
        part = root
    end

    return part
end

local function getLocalRoot()
    local character = LocalPlayer.Character

    if character == nil then
        return nil
    end

    return character:FindFirstChild("HumanoidRootPart")
end

local function makeBox()
    local box = Drawing.new("Square")
    box.Filled = false
    box.Thickness = 3
    box.Transparency = 1
    box.Color = Color3.fromRGB(255, 40, 40)
    box.Visible = false
    box.ZIndex = 50

    return box
end

local function makeLabel()
    local label = Drawing.new("Text")
    label.Size = 16
    label.Center = true
    label.Outline = true
    label.Transparency = 1
    label.Color = Color3.fromRGB(255, 255, 255)
    label.Visible = false
    label.ZIndex = 51

    return label
end

local function destroyLatch(player)
    local data = latched[player]

    if data == nil then
        return
    end

    if data.Box ~= nil then
        pcall(function()
            data.Box:Remove()
        end)
    end

    if data.Label ~= nil then
        pcall(function()
            data.Label:Remove()
        end)
    end

    latched[player] = nil
end

local function clearAll()
    local list = {}

    for player, _ in pairs(latched) do
        list[#list + 1] = player
    end

    local i = 1

    while i <= #list do
        destroyLatch(list[i])
        i = i + 1
    end

    print("[StickyHitboxes V10] Cleared")
end

local function nearestBodyPartDistance(character, hitboxPosition)
    local children = character:GetChildren()
    local bestDistance = math.huge
    local bestPart = nil

    local i = 1

    while i <= #children do
        local child = children[i]

        if child:IsA("BasePart") then
            local distance = (child.Position - hitboxPosition).Magnitude

            if distance < bestDistance then
                bestDistance = distance
                bestPart = child
            end
        end

        i = i + 1
    end

    return bestDistance, bestPart
end

local function findOwnerPlayer(hitbox)
    local owner = hitbox:FindFirstChild("Owner")

    if owner == nil then
        return nil, nil
    end

    if owner.ClassName ~= "StringValue" then
        return nil, nil
    end

    if owner.Value == nil or owner.Value == "" then
        return nil, nil
    end

    local players = Players:GetPlayers()
    local i = 1

    while i <= #players do
        local player = players[i]

        if player ~= LocalPlayer then
            if player.Name == owner.Value or player.DisplayName == owner.Value then
                local character, humanoid, root = getCharacterData(player)

                if character ~= nil and humanoid ~= nil and root ~= nil then
                    local _, part = nearestBodyPartDistance(
                        character,
                        hitbox.Position
                    )

                    return player, part
                end
            end
        end

        i = i + 1
    end

    return nil, nil
end

local function findTarget(hitbox)
    local ownerPlayer, ownerPart = findOwnerPlayer(hitbox)

    if ownerPlayer ~= nil then
        print(
            "[StickyHitboxes V10] Matched Owner -> "
            .. tostring(ownerPlayer.Name)
        )

        return ownerPlayer, ownerPart
    end

    local localRoot = getLocalRoot()

    if localRoot == nil then
        return nil, nil
    end

    local players = Players:GetPlayers()

    local bestPlayer = nil
    local bestPart = nil
    local bestDistance = math.huge

    local i = 1

    while i <= #players do
        local player = players[i]

        if player ~= LocalPlayer then
            local character, humanoid, root = getCharacterData(player)

            if character ~= nil and humanoid ~= nil and root ~= nil then
                local localDistance =
                    (root.Position - localRoot.Position).Magnitude

                if localDistance <= MAX_DISTANCE then
                    local bodyDistance, bodyPart =
                        nearestBodyPartDistance(
                            character,
                            hitbox.Position
                        )

                    if bodyDistance < bestDistance then
                        bestDistance = bodyDistance
                        bestPlayer = player
                        bestPart = bodyPart
                    end
                end
            end
        end

        i = i + 1
    end

    if bestPlayer == nil then
        return nil, nil
    end

    local allowedDistance =
        MAX_MATCH_DISTANCE + (hitbox.Size.Magnitude * 0.5)

    if bestDistance > allowedDistance then
        print(
            "[StickyHitboxes V10] Closest target too far from hitbox: "
            .. tostring(math.floor(bestDistance * 100) / 100)
        )

        return nil, nil
    end

    print(
        "[StickyHitboxes V10] Matched -> "
        .. tostring(bestPlayer.Name)
        .. " | distance="
        .. tostring(math.floor(bestDistance * 100) / 100)
    )

    return bestPlayer, bestPart
end

local function latchPlayer(player, hitbox, matchedPart)
    local character, humanoid, root = getCharacterData(player)

    if character == nil or humanoid == nil or root == nil then
        return
    end

    -- One box per player: newest hitbox completely replaces the previous one.
    destroyLatch(player)

    local anchor = matchedPart

    if anchor == nil or anchor.Parent ~= character then
        anchor = getBodyAnchor(character, root)
    end

    if anchor == nil then
        return
    end

    latched[player] = {
        HitboxSize = hitbox.Size,
        AnchorPartName = anchor.Name,
        Box = makeBox(),
        Label = makeLabel()
    }

    print(
        "[StickyHitboxes V10] LATCHED -> "
        .. tostring(player.Name)
        .. " | anchor="
        .. tostring(anchor.Name)
        .. " | size="
        .. tostring(hitbox.Size)
    )
end

local function hideLatch(data)
    if data.Box ~= nil then
        data.Box.Visible = false
    end

    if data.Label ~= nil then
        data.Label.Visible = false
    end
end

local function updateLatch(player, data)
    local character, humanoid, root = getCharacterData(player)

    if character == nil or humanoid == nil or root == nil then
        destroyLatch(player)
        return
    end

    local anchor = character:FindFirstChild(data.AnchorPartName)

    if anchor == nil or not anchor:IsA("BasePart") then
        anchor = getBodyAnchor(character, root)

        if anchor == nil then
            destroyLatch(player)
            return
        end

        data.AnchorPartName = anchor.Name
    end

    local screenPosition, onScreen =
        WorldToScreen(anchor.Position)

    if screenPosition == nil or onScreen ~= true then
        hideLatch(data)
        return
    end

    local distance = 10

    if Camera ~= nil and Camera.CFrame ~= nil then
        local cameraPosition = Camera.CFrame.Position

        if cameraPosition ~= nil then
            distance =
                (cameraPosition - anchor.Position).Magnitude
        end
    else
        local localRoot = getLocalRoot()

        if localRoot ~= nil then
            distance =
                (localRoot.Position - anchor.Position).Magnitude
        end
    end

    if distance < 1 then
        distance = 1
    end

    local sizeMagnitude = data.HitboxSize.Magnitude

    local pixelSize =
        (sizeMagnitude * 115) / distance

    if pixelSize < 24 then
        pixelSize = 24
    end

    if pixelSize > 220 then
        pixelSize = 220
    end

    local width = pixelSize
    local height = pixelSize

    -- Roblox avatars are vertically taller than a typical hitbox.
    if data.HitboxSize.Y >= data.HitboxSize.X then
        height = pixelSize * 1.15
    end

    local x = screenPosition.X - width * 0.5
    local y = screenPosition.Y - height * 0.5

    data.Box.Position = Vector2.new(x, y)
    data.Box.Size = Vector2.new(width, height)
    data.Box.Visible = true

    data.Label.Position =
        Vector2.new(
            screenPosition.X,
            y - 18
        )

    data.Label.Text =
        tostring(player.DisplayName)
        .. " [HITBOX LOCKED]"

    data.Label.Visible = true
end

local function cleanup()
    local stale = {}

    for player, _ in pairs(latched) do
        local character, humanoid, root =
            getCharacterData(player)

        if player.Parent ~= Players then
            stale[#stale + 1] = player
        elseif character == nil
            or humanoid == nil
            or root == nil then

            stale[#stale + 1] = player
        end
    end

    local i = 1

    while i <= #stale do
        destroyLatch(stale[i])
        i = i + 1
    end
end

local function processHitboxes()
    local folder =
        Workspace:FindFirstChild("Hitboxes")

    if folder == nil then
        if lastFolderState ~= false then
            print(
                "[StickyHitboxes V10] workspace.Hitboxes NOT FOUND"
            )

            lastFolderState = false
        end

        return
    end

    if lastFolderState ~= true then
        print(
            "[StickyHitboxes V10] workspace.Hitboxes FOUND"
        )

        lastFolderState = true
    end

    local children = folder:GetChildren()
    local current = {}

    local i = 1

    while i <= #children do
        local hitbox = children[i]

        if hitbox:IsA("BasePart") then
            local key = hitbox.Address

            if key == nil then
                key = tostring(hitbox)
            end

            current[key] = true

            if seen[key] == nil then
                seen[key] = true

                local target, part =
                    findTarget(hitbox)

                if target ~= nil then
                    latchPlayer(
                        target,
                        hitbox,
                        part
                    )
                end
            end
        end

        i = i + 1
    end

    local staleSeen = {}

    for key, _ in pairs(seen) do
        if current[key] == nil then
            staleSeen[#staleSeen + 1] = key
        end
    end

    i = 1

    while i <= #staleSeen do
        seen[staleSeen[i]] = nil
        i = i + 1
    end
end

task.spawn(function()
    while RUNNING do
        task.wait()

        if ENABLED then
            local ok, err = pcall(function()
                cleanup()
                processHitboxes()

                local players = {}

                for player, _ in pairs(latched) do
                    players[#players + 1] =
                        player
                end

                local i = 1

                while i <= #players do
                    local player = players[i]
                    local data = latched[player]

                    if data ~= nil then
                        updateLatch(
                            player,
                            data
                        )
                    end

                    i = i + 1
                end
            end)

            if not ok then
                warn(
                    "[StickyHitboxes V10] "
                    .. tostring(err)
                )
            end
        end
    end
end)

function StickyHitboxes.SetEnabled(value)
    ENABLED = value == true

    if ENABLED == false then
        clearAll()
    end
end

function StickyHitboxes.IsEnabled()
    return ENABLED
end

function StickyHitboxes.SetDetectionDistance(value)
    local newDistance = tonumber(value)

    if newDistance ~= nil
        and newDistance > 0 then

        MAX_DISTANCE = newDistance
    end
end

function StickyHitboxes.SetMatchDistance(value)
    local newDistance = tonumber(value)

    if newDistance ~= nil
        and newDistance > 0 then

        MAX_MATCH_DISTANCE =
            newDistance
    end
end

function StickyHitboxes.Clear()
    clearAll()
end

function StickyHitboxes.GetLatchedCount()
    local count = 0

    for _, _ in pairs(latched) do
        count = count + 1
    end

    return count
end

function StickyHitboxes.Destroy()
    ENABLED = false
    RUNNING = false
    clearAll()
end

print("[StickyHitboxes V10] 2D BODY-LOCK BUILD LOADED")
print("[StickyHitboxes V10] No 3D vector constructor usage")
print("[StickyHitboxes V10] One newest box per player")

return StickyHitboxes
